[build]
builder = "heroku.buildpacks"

[deploy]
numReplicas = 1
restartPolicyMaxRetries = 0

[[services]]
name = "app"
buildCommand = "pip install -r requirements.txt"
startCommand = "streamlit run app_treinador.py --server.port=$PORT --server.address=0.0.0.0"
